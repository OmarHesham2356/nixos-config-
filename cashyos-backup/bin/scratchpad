#!/bin/fish
if hyprctl clients | grep -q "class: scratchpad"
  hyprctl dispatch togglespecialworkspace scratchpad
else
  kitty --class scratchpad &
  sleep 0.3
  hyprctl dispatch togglespecialworkspace scratchpad
end
