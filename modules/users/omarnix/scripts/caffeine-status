#!/usr/bin/env bash
# Check if caffeine mode is active
if [ -f /tmp/caffeine.pid ]; then
  pid=$(cat /tmp/caffeine.pid)
  if kill -0 "$pid" 2>/dev/null; then
    echo true
  else
    echo false
  fi
else
  echo false
fi
